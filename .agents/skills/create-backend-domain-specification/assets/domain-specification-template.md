# [ドメイン名] ドメインモデル仕様書

<!--
作成時の注意:
- Domainエラー名とRepository Protocol名はクリーンアーキテクチャ上の契約として扱う。
- 特定のプログラミング言語、DB、SDK、フレームワークの実装詳細は記載しない。
- 仕様を実現するための実装上の工夫は、仕様書ではなく該当コードのコメントへ記載する。
-->

## 1. 概要

- 目的: [このドメインが表現・保証する業務領域]

## 2. 対象範囲

### 対象

- [このドメインに含む概念・業務ルール]

### 対象外

- [このドメインに含まない概念・業務ルール]

## 3. 用語

| 用語 | 定義 |
| --- | --- |
| [用語] | [業務上の定義] |

## 4. Aggregate

個別のAggregate境界は、同一トランザクションで保証する整合性、ライフサイクル、サイズ、性能を根拠として定義する。

### [Aggregate名]

- Aggregate Root: [Root Entity]
- 内部Entity・Value Object: [構成要素。ない場合は「なし」]
- 境界の根拠: [同一トランザクションで保証する必要がある整合性]
- 外部からの操作方法: [Aggregate Rootを経由する操作]
- 他Aggregateとの関係: [識別子による参照など。ない場合は「なし」]

## 5. Entity

### [Entity名]

- 種別: [Aggregate Root / Aggregate内部Entity / 独立Entity]
- 識別子: [識別子の型。生成方法が未決定の場合は未確定]
- 責務: [表現する業務概念と保証するルール]

#### 属性

| 属性名 | 型 | 必須 | 説明・制約 |
| --- | --- | --- | --- |
| [属性名] | [Domain型] | [必須/任意] | [業務上の意味・制約] |

監査目的だけの属性やORM・DB都合の属性は記載しない。業務上の概念として必要な日時・履歴情報は記載する。

#### 操作

| 操作 | 入力 | 結果 | 関連するDomainルール | 送出するDomainエラー |
| --- | --- | --- | --- | --- |
| [操作] | [Domain型] | [状態変更または戻り値] | [DR-XX] | [具体的なDomainエラー] |

## 6. Value Object

### [Value Object名]

- 表現する値: [業務上の意味]
- 保持する値: [プリミティブ型または他のValue Object]
- 等価性: [値による等価性の基準]
- 制約: [生成時に検証する不変条件]
- 振る舞い: [値に関する業務操作]
- 違反時のDomainエラー: [具体的なDomainエラー]

業務上の意味、制約、または振る舞いを持つ値のみValue Objectとして定義する。列挙値をValue Objectにするかは、その値が制約や振る舞いを持つかに基づいて判断する。

## 7. Domainルール

どのユースケースから呼ばれても常に成立する不変条件と状態遷移のみを記載する。ユースケース固有の条件はユースケース仕様書へ記載する。

### DR-01: [ルール名]

- 対象: [Entity / Value Object / Aggregate]
- 内容: [常に満たす条件、または許可される状態遷移]
- 違反時のDomainエラー: [具体的なDomainエラー]

## 8. Domainエラー

| エラー | 発生条件 | 呼び出し側が区別する理由 |
| --- | --- | --- |
| [Domainエラー] | [業務ルール違反] | [必要な対応差] |

`DomainError`自体は直接送出しない。

## 9. Command Repository Protocol

Repositoryは原則としてAggregate Root単位で定義する。

| Protocol | 操作 | 引数 | 戻り値 | 送出するエラー |
| --- | --- | --- | --- | --- |
| [Protocol名] | [Aggregate Root単位の操作] | [Domain型] | [Domain型またはなし] | [契約上のエラー] |

読み取り専用のQuery Repository ProtocolはApplication層に定義するため、本節には記載しない。

## 10. 関連するDomain Service

- [Domain Service仕様書のパス。該当なしの場合は理由]

## 11. テスト観点

- 正常系: [有効な生成・操作・状態遷移]
- 境界値: [制約の直前・境界・直後]
- 異常系: [不変条件違反・禁止された状態遷移と具体的なDomainエラー]
- Aggregate: [Root経由でのみ操作できること、Aggregate内の整合性]

## 12. 関連仕様書

- ユースケース仕様書: [パス。存在しない場合は不足を記載]
- 外部接続仕様書: [パス。該当なしの場合は理由]

## 13. 未確定事項

- [確認が必要な事項。ない場合は「なし」]
